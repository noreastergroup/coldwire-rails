# frozen_string_literal: true

# Bundler requires a gem by its own name, so this file has to exist under the gem name.
# Everything lives in `Coldwire` — the gem is called coldwire-rails the way turbo-rails
# provides `Turbo` and importmap-rails provides `Importmap`.
require "coldwire"
